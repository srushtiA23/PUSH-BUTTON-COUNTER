# PUSH-BUTTON-COUNTER
This project demonstrates a simple embedded system using an Arduino Uno (ATmega328P) to count the number of times a push button is pressed. Software debouncing is implemented to eliminate false counts caused by mechanical switch bouncing. The current count is displayed on the Serial Monitor and can also be extended to a 16×2 LCD display.
